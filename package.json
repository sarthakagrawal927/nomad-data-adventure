{
  "name": "nomad-data-adventure",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --check app.js && node --check model.js && node --test tests/*.test.mjs && node scripts/build.mjs",
    "dev": "python3 -m http.server 4173 --bind 127.0.0.1",
    "deploy": "pnpm run check && bash ../saas-maker/tooling/scripts/fleet-deploy-guard.sh nomad-data-adventure && wrangler pages deploy dist --project-name nomad-data-adventure --branch main"
  },
  "devDependencies": {
    "wrangler": "4.131.1"
  }
}
